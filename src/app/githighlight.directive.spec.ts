import { GithighlightDirective } from './githighlight.directive';

describe('GithighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new GithighlightDirective();
    expect(directive).toBeTruthy();
  });
});
